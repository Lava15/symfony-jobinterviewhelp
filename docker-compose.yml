version: '3.9'
networks: 
  app-network: 
    driver: bridge
services:
  php: 
    container_name: php
    build: 
      context: .docker/local/
      dockerfile: php.dockerfile
    volumes: 
      - .:/var/www/html
    ports: 
      - 9000:9000
    networks: 
      - app-network
